import React from 'react';
import Header from '../Header/Header';

const Settings = () => {
    return(
        <div>
            <Header/>
            <h1>Settings</h1>
        </div>
    );
}

export default Settings;